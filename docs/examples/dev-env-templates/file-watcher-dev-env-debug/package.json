{
  "name": "simplest-ts-setup",
  "version": "1.0.0",
  "description": "TypeScript needs to be compiled to JavaScript before it can be run. This mini-project shows how to setup a minimal TypeScript project with one module and one two files that use the module. This will let us evolve the setup in other folders to add features.",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "nodemon --exec ts-node src/Server.ts",
    "dev": "nodemon src/Index.ts",
    "dev:debug": "nodemon --inspect src/Server.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.21",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "@types/express": "^4.17.17",
    "express": "^4.18.2"
  }
}
